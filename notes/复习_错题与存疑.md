# 复习：错题 + 存疑（第 6–9 天 + 两数之和）

只看这一份。对过的题不重复列。

---

## bool 是什么

`bool` = 布尔类型，只有两个值：**`True`** 和 **`False`**（首字母必须大写）。

用来表示「是 / 否」，常见出现在 `if` 的条件里：

```python
is_student = True
if is_student:
    print("是学生")
```

`==`、`>=`、`in` 的结果也是 bool：`score >= 60` 要么 `True` 要么 `False`。

---

## 口述第三问：`[]` 什么时候造 list，什么时候取值

- **单独出现、用来赋值造容器** → 造 list：`scores = [90, 85]`
- **写在已经有的变量后面** → 取值（查），不是造 list：
  - `scores[0]`：list / tuple 按下标查
  - `grade["李宏"]`：dict 按键查

---

## 第 6 天

| 题 | 你当时 | 答案 | 记住 |
|----|--------|------|------|
| 选择 5 | 先选 C，后改 B | **B `"53"`** | 引号包着的是字符串，`+` 是拼接，不是算术 |
| 选择 7 | 先选 D，后改 A | **A `2`** | `len("李宏")` 按**字符个数**，两个汉字就是 2 |

编码：日常直接写中文字符串；读文件再写 `encoding="utf-8"`。`ex04` 可继续跳过。

---

## 第 7 天

| 题 | 你当时 | 答案 | 记住 |
|----|--------|------|------|
| 选择 1 | 写成 A-B | **B** | A 是 tuple `()`，list 必须 `[]`，中间要逗号 |
| 选择 6 | 对，并问 list/tuple 括号 | **B** | 造 list=`[]`（可改）；造 tuple=`()`（一般不能改） |
| 选择 9 | 猜 B（没见过规则） | **B `[1,2,3,4]`** | list 的 `+` 是**接在后面**，不是对应相加 |
| 判断 3 | 存疑，后改错 | **错** | `=` 赋值；`==` 判断是否相等。`if` 里必须用 `==` |
| 判断 6 | 先写对，后改必须缩进 | **错**（原句「可以不缩进」是错的） | `if` 下面那一行**必须缩进**，哪怕只有一行 |

---

## 第 8 天

| 题 | 你当时 | 答案 | 记住 |
|----|--------|------|------|
| 选择 3 | A（3） | **B（2）** | `{"李宏","李宏","王五"}` 是 set，重复丢掉 |
| 选择 5 | 存疑 | **B 空 dict** | `[]` 空 list；`()` 空 tuple；`{}` 空 dict；空 set 写 `set()` |
| 选择 7 | C（match） | **A `for s in subjects`** | 遍历 list 用 for；match 第一遍不做 |
| 判断 3 | 对 | **错** | `grade["李宏"]` 的 `[]` 是**按键取值** |
| 判断 4 | 错 | **对** | 空 set 只能 `set()`，不能 `{}` |
| 判断 5 | 觉得错 | **对** | 缩进 = 前面多出来的空格，表示「属于上面的 if/for」 |

---

## 第 9 天（函数参数，存疑）

| 题 | 你当时 | 答案 | 记住 |
|----|--------|------|------|
| 小结「命名关键字」 | 问为什么必须 `city="杭"` | `*` 后面的不能按位置递 | `*` 是挡板：后面只能 `名字=值` |

`def f(a, *, city)` 里，`*` 不是参数，是挡板。挡板前的 `a` 可以按顺序递；挡板后的 `city` 必须写成 `city=...`，写成 `f(1, "杭")` 会报错。

---

## 两数之和（写法坑）

- `for x in nums` 不要写成 `nums()`（list 不是函数）
- 变量名用 `nums`，不要写成 `num[i]`
- `if` 后面要 `:`，成立时的 `print` / `return` 必须缩进在 `if` 里面
- 要下标：`for i in range(len(nums))`，内层 `range(i+1, n)`
- `return [i, j]` 放在 `if` 里；本地练习用 `print`，不要把 `return` 写在循环外面

通解骨架：

```python
n = len(nums)
for i in range(n):
    for j in range(i + 1, n):
        if nums[i] + nums[j] == target:
            return [i, j]
```
