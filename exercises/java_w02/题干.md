# java_w02 题干一览（IDEA 可直接打开本文件看题）

| 文件 | 题目 |
|------|------|
| `Ex01Switch.java` | 用 **switch** 根据 `day`（1~7）打印星期一…日；其它打印「无效日期」 |
| `Ex02MethodAvg.java` | 写 **`average(int[] scores)`**，返回平均分；main 里打印 `平均分: 86.6` |
| `Ex03CountPass.java` | 写 **`countPass(int[] scores)`**，统计 ≥60 的个数；打印 `及格人数: 3` |

每个 `.java` 文件**最上面的 `/* 题干 */` 注释**也有完整说明；运行时第一行也会打印题目摘要。

若 IDEA 左侧看不到新文件：**File → Open** 选本文件夹 `java_w02`，或右键项目 → Reload from Disk。
